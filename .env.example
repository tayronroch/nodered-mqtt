# Bcrypt hash da senha do admin do Node-RED
# Gere com: node -e "require('bcryptjs').hash('sua-senha', 8).then(console.log)"
NODE_RED_ADMIN_PASSWORD_HASH=

# Dominio da aplicacao (ex: nodered.example.com)
DOMAIN=
